// Package services implements Momobase's application-level business workflows.
//
// It coordinates authentication, provider administration and execution, payment
// routing, transaction reconciliation, webhook processing, health checks, and
// audit recording across the domain, provider, platform, and persistence layers.
package services
