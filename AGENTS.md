# Repository instructions

## Commits

- Use Conventional Commits scoped to the affected top-level folder, such as `feat(web): simplify public website`. Use `root` for repository-root files, such as `docs(root): document commit convention`.
