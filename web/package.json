{
  "name": "@momobase/web",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root for everything Momobase ships to a browser.",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=22"
  }
}
