{
  "name": "@momobase/web",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root for the embedded Momobase dashboard.",
  "scripts": {
    "build": "pnpm -r run build",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10"
  }
}
